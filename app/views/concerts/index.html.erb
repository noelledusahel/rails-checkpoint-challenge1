<h1>Thud Hub</h1>

<%= link_to 'Add New Concert', new_concert_path  %> 

<div class="concert-list">
	<% @concerts.each do |concert| %> 
	<p><b><%= link_to concert.band, concert_path(concert) %></b> 
	Venue: <%= concert.venue  %> </p>
		<% end  %>
	</div>

